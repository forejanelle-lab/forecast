export function getSubmissionRecommendationReasons(
  actorId: string,
  roleId: string,
  projectId: string,
): string[] {
  void actorId;
  void roleId;
  void projectId;
  return [];
}

export function isRecommendedSubmissionActor(
  actorId: string,
  roleId: string,
  projectId: string,
): boolean {
  void actorId;
  void roleId;
  void projectId;
  return false;
}
